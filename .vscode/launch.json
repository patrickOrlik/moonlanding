{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/a.exe",  // change to a.out for other OSs
            "args": [],
            "preLaunchTask": "${defaultBuildTask}",
        }
    ]
}